#include <iostream>
#include <stack>
using namespace std;

int main()
{
	int n;
	stack <int> s;
	int count = 0;
	cin >> n;
	
	for (int i = 0; i < n; i++) {
		while(s.empty() && )
	}

	cout << count;
}